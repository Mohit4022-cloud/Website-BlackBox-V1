{
  "name": "mohit-ai-website",
  "version": "1.0.0",
  "description": "Mohit AI Marketing Website",
  "scripts": {
    "dev": "python3 -m http.server 8000 --directory user-workspace",
    "start": "python3 -m http.server $PORT --directory user-workspace",
    "build": "echo 'Static site, no build needed'",
    "preview": "python3 -m http.server 8000 --directory user-workspace"
  }
}
